<h2>Listing bats added/removed to protocol <em>"<%= @protocol.title %>"</em> between: <br>
<%= @start_date.strftime("%B %Y") %> and <%= @end_date.strftime("%B %Y") %></h2>

<% for hist in @p_hist %>
<%= link_to hist.bat.band, :controller => :bats, :action => :show, :id => hist.bat %><br>
<% end %>

<p>
note: does <b>not</b> include bats that were added to protocol before these dates and removed after these dates
</p>

<br>
<div style="width:700px; margin-right:auto; margin-left:auto">
<%= render :partial => 'form_to_show_bats_on_protocol_when', :locals =>{:protocol=>@protocol} %>
</div>

<%= render :partial => 'curr_bats_on_protocol', :locals => {:protocol => @protocol} %>

<%= nice_date @end_date %>